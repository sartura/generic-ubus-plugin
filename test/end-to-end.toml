### Information for opening a SSH session to the device ###
[login]
    address = "192.168.1.1:830"
    username = "root"
    password = "root"
    enabled = true

### YANG module configuration ###
[[test]]
    XMLRequestBody = """
    <edit-config>
        <target>
            <running/>
        </target>
        <config>
            <generic-ubus-config xmlns="https://terastream/ns/yang/terastream-generic-ubus">
                <ubus-object>
                    <name>router.wireless</name>
                    <yang-module>router-wireless</yang-module>
                    <method>
                        <name>status</name>
                        <message>{}</message>
                    </method>
                </ubus-object>
            </generic-ubus-config>
        </config>
    </edit-config>
    """
    XMLResponse = """<ok/>"""

### get the written config
### delete the data

### install all
### get all
### delete all

### ubus call one
### ubus call multiple

### module install one
### module instal multiple

### feature enable - weather report just for me use other to test
### feature enable multiple

### filter one do get all
### filter all do get all